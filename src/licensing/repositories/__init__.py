"""Repository modules (one per aggregate) land here in Phase C onward:
users.py, organizations.py, licenses.py, devices.py, activation.py,
certificates.py. Phase A intentionally ships only the models these will
query — see README.md phase plan.
"""
